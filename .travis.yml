language: csharp
mono: none
dotnet: 3.1.403
script:
  - dotnet build || travis_terminate 1;
  - dotnet publish server -r win-x64 -o server/deploy -c Release
  - pushd server/deploy
  - zip -r9 ../../deploy.zip *
  - popd
